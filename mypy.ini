[mypy]
python_version = 3.12
strict = True
# HA and third-party libraries have no bundled stubs when running outside
# the HA venv. Suppress import-not-found for all of them; our own code is
# still fully checked.
ignore_missing_imports = True
